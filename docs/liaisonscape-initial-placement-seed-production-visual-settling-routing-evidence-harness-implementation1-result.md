# E2R-LIAISONSCAPE-INITIAL-PLACEMENT-SEED-PRODUCTION-VISUAL-SETTLING-ROUTING-EVIDENCE-HARNESS-IMPLEMENTATION1

Date: 2026-09-03

Status: `HARNESS IMPLEMENTATION1 CANDIDATE`

## Case / Decision

`CASE 1 / Decision A`: the disposable visual-evidence harness implementation
is validated for implementation use. Historical materializer semantics were
revalidated and future evidence is explicitly rebound to Product authority
`563d5bc3ce8d3a915c75dd13b33188ae6f7fccaf`. The harness candidate is not
adopted to Live.

PR-3 remains `NO`. No full candidate matrix, V0–V3 PR-3 records, browser
execution, screenshot, human visual review, candidate ranking, candidate
selection, or Product candidate integration occurred.

## Source-drift reconciliation

Harness Design1 froze Product at historical commit
`fd563340625fd3d88dc25baedc93c4f8fe69e5e7`. The accepted Product seam has
since advanced through 1B–1G and is now authoritative at
`563d5bc3ce8d3a915c75dd13b33188ae6f7fccaf` (`test: adopt isolated evaluation
browser entry`). The classifications are independent:

- `MATERIALIZER-SOURCE-DRIFT = UNAFFECTED`: the retained materializer source
  bytes and semantics are independent of Product runtime source. Its 9-file
  snapshot matched byte-for-byte and its original tests and bounded outputs
  reproduced.
- `PRODUCT-DOWNSTREAM-SOURCE-DRIFT = REVALIDATED / NEW EVIDENCE AUTHORITY`:
  current V1/V2/V3 infrastructure records Product authority 563d5bc and uses
  the accepted explicit-seed, routing, label, fit, viewport, and wrapper seams.
- `HARNESS-SOURCE-DRIFT-REVALIDATION1 = PASS`.

There is no evidence mixing: old `fd563340` is retained only as historical
materializer/Harness Design1 provenance; no old visual evidence exists, and
new future records must carry 563d5bc.

The retained materializer at
`C:\Users\extra\E2R\e2r-liaison-scape-production-candidate-materialization-implementation1`
was not modified. Its pre-existing `experimental/` state was preserved.

## Disposable harness and snapshot

The new clone is
`C:\Users\extra\E2R\e2r-liaison-scape-visual-evidence-harness-implementation1`.
It was created from committed source only at initial HEAD
`563d5bc3ce8d3a915c75dd13b33188ae6f7fccaf` and did not copy Live working-tree
sample bytes.

The exact materializer snapshot is under
`experimental/production-candidate-materialization/`, with provenance in
`source-provenance.json`. The copied files and SHA-256 values are:

| Path | SHA-256 |
| --- | --- |
| `core/rational.mjs` | `948EDDE70FE80633E39D02E51BD5CA21333825F6E32DD4FB88777736A05A85ED` |
| `core/graph.mjs` | `899472982A7EE2DBBF32F5F1640202DC20BFF7E8BD4424048997137B2076B15E` |
| `core/fp1.mjs` | `3B1B7567D5A56BDE6DF66B2771E0C5AE1F7190483DA2E6BB6DCECBB587BBFBF6` |
| `core/canonical.mjs` | `553DE8A41AE94E6D9258D783FDB6117C25C10B1DE97E342D57D6D5A75C5629F0` |
| `core/gates.mjs` | `069DA87471E092E76629B3C213AAA3832AA7E146F96187CFD44913396E99398C` |
| `core/materializer.mjs` | `850E9F9D8F6624DD9BF4B3ABC6DC6DC143037F8BDA0AE357F3C8924E502E8CF4` |
| `fixtures/authoritative.mjs` | `A72475F2450D93EFEAAB4CC8655FA72440B6472917615953AD4824E955D62BD5` |
| `tests/materialization.test.mjs` | `6A2AB8E448B665067FB2E07F6F21DD1F92945B3442EC9421A921DC1950375BE2` |
| `harness/run-validation.mjs` | `D06A2DB57C74D852DD962CB541FB94D178985517A5EA1DF861BAF54DB272C454` |

The original pure materializer suite passed `13/13`. The bounded historical
Lighthouse/Ashen Crown/K3 × five identity runner reproduced `15/15`; these
were implementation-validation rechecks, not PR-3 evidence. No historical
generated artifacts were copied as current evidence.

The frozen role manifest is
[`docs/evidence/visual-fixture-role-manifest-v1.json`](evidence/visual-fixture-role-manifest-v1.json),
SHA-256 `B867E9FF7B84107E8B4DDCCC02458B7B9AA535AB6FACE4AD10A2942CC8C8D8F5`.
The loader verifies the manifest, 8 structural fixture IDs, 16 EN/JA
authorities, raw hashes, projection hashes, source/path metadata,
structural pairing, locale/presentation variant, and role references before a
cell can run. All authority checks passed.

## Harness implementation

The candidate commit freezes `VSR-HARNESS-IMPLEMENTATION-v1` and
`VSR-RUN-PLAN-v1` in
`experimental/product-evaluation-seam/visual-evidence-harness1/`.

- Registry: exactly `TA0/REP-0`, `TA2-DEG-W1-OP-v1`,
  `TA2-MOTIF-W1-OP-v1`, `TA3-FWD-W1-OP-v1`, and `TA3-REV-W1-OP-v1`, with
  their exact materializer IDs. TA1, K3, FP1, unknown, duplicate, and
  unlisted identities are rejected without fallback.
- Plan: V0 `40`, V1 `40`, V2 `80`, V3 `80`; candidate total `240`. B0 is
  `8`; B1 is `32`; diagnostic total `40`. The plan is declarative and has no
  score, rank, winner, best-candidate, preference, or majority field.
- Configuration is fail-closed: `PR3-RUNTIME-TRANSFORM = UNFROZEN`.
  `100/1, 200/1, 10/1` was used only as explicitly labeled
  `IMPLEMENTATION-VALIDATION` test configuration.
- V0 calls only the exact materializer snapshot and records exact coordinate,
  candidate/materializer/family identity, rational transform, fingerprint,
  gates, source hashes, and failure. It does not settle, route, label, or
  render.
- V1 calls the accepted current Product
  `experimental/product-evaluation-seam/explicit-seed-adapter1/adapter.ts`
  through a runtime facade, with the accepted 3-iteration configuration. It
  records the exact V0 fingerprint, Product authority, adapter version,
  finite/distinct movement diagnostics, and no-mutation evidence. It does not
  regenerate a seed.
- V2 calls current `buildEntityGraph`, Product `placeNodeLabel` for the
  provisional route input, `deriveAutomaticRoutes`,
  `deriveAutomaticRelationLabels`, `deriveAutomaticNodeLabels`, the Product
  line-style/arrow-display/arrowhead helpers, `fitGraphView`, and
  `centeredViewportTransform`. Manual routes, manual labels, active drag,
  selection, persistence, and rescue are empty/absent.
- `VSR-BROWSER-PACKET-v1` contains derived V2 data for the accepted
  `render-wrapper1`; the browser entry contains no candidate algorithm.
- `VSR-CAPTURE-DRIVER-v1` is Node-side, import-side-effect-free, and requires
  explicit runner injection. Its canonical viewport is 1280×900 CSS px,
  DSF 1, zoom 100%, SVG viewBox `0 0 800 500`. It was not invoked.
- `VSR-ARTIFACT-CANONICAL-v1` writes deterministic UTF-8/LF/final-newline
  artifacts and excludes generatedAt, temporary paths, PIDs, and debug ports
  from semantic indexing. `VSR-STAGE-LINKAGE-v1` carries exact upstream
  fingerprints and rejects silent recomputation.
- Failure statuses and attribution are explicit for candidate, harness,
  fixture authority, Product downstream, browser capture, and missing
  evidence. Review packet plumbing uses stable local A–E IDs and contains no
  ranking or winner semantics. B0/B1 remain current-Product diagnostics with
  no candidate execution.

## Validation

The harness implementation test file reports T1–T17 and H-T18–H-T28 as
`28/28 PASS`. It exercises all 16 authority entries, exact plan cardinality,
registry rejection, tiny V0→V1→V2 linkage, packet/capture/artifact/review
contracts, B0/B1 separation, and normal-source reachability.

The harness browser build passed with existing Vite 8.2.1 using a temporary
output directory; the bundle dependency audit found no App, DatasetService,
materializer, selector UI, localStorage, capture-driver, CDP, Playwright, or
Puppeteer references. The temporary output was removed. No browser was
launched and no screenshot/PNG was created.

Normal Product source cannot reach the harness or materializer. The browser
entry reaches the accepted render wrapper, but not App, normal main,
Dataset/persistence lifecycle, candidate execution, or Node capture code.
The accepted current gates remain: wrapper `13/13`, 1G isolation `7/7`,
APP-RENDER-EQUIVALENCE `PASS`, PRODUCT-CSS-PROVENANCE `PASS`,
graph-presentation `22/22`, pure routing `9/9`, App routing `16/16`, pure
labels `8/8`, App labels `16/16`, auto-layout `38/38`, relation/node reversal
`16/16` solve and settle, fresh-process `PASS`, no-mutation `PASS`, and
baseline-v2 SHA
`F18C4C98607B9412BD6C035803002BE1DFEE1F2AF721942C8D8FF69A5496795B`.
The current Product suite passed `309/309`; lint, normal build, and diff-check
passed.

## Protected state and next step

The harness candidate commit is
`36650462f7f2def95478098a01cb345b9c89cfa1`, subject
`test: implement LiaisonScape visual evidence harness`, parent
`563d5bc3ce8d3a915c75dd13b33188ae6f7fccaf`. It is local, unpushed, and not
adopted to Live. Its worktree is clean.

Live remains at HEAD `563d5bc3ce8d3a915c75dd13b33188ae6f7fccaf` with only the
protected EN/JA sample modifications. Their final hashes remain:

- EN `19B6362468CF51E32FA9DEA016BC4211D8055C299E28D0CC402BC63B5A5B31D1`
- JA `C4604679A8D730705E33E51F06BAED46E37BD571C30595145D1938CD3E9B4EFA`

No Product source, package, adopted seam, baseline, retained materializer,
prior provenance clone, Session-0052, or ai-knowledge pre-existing change
was modified. No push, tag, release, deploy, or publication occurred.

`V0/V1/V2/V3 PR-3 evidence = NONE`, `E-VISUAL = NO`, candidate selected =
`NO`, Product integration = `NO`, and `PR-3 = NO`.

Knowledge Candidate: no new reusable workspace knowledge entry was needed;
the result is fully recorded in this specification result and the harness
provenance manifest.

The exact next evidence checkpoint is not started. Because the accepted
Harness Design1 and current roadmap do not yet freeze a post-implementation
run-configuration/evidence name, record:

`NEXT EVIDENCE CHECKPOINT NAME REQUIRES ROADMAP/DESIGN RECONCILIATION`

The prior `...EVIDENCE-EXECUTION1` pointer remains deferred and was not run.
