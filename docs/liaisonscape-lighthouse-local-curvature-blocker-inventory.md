# LiaisonScape Lighthouse Local Curvature Blocker Inventory

Date: 2026-09-01

## Scope and method

This is a read-only inventory of the current dirty Japanese Lighthouse sample
used by localhost. A temporary detached worktree read that exact main file;
the main sample was not edited, staged, or committed. A temporary observer in
the actual `routeGraphEdge` candidate loop recorded lower-curvature candidates,
non-label safety, and Node-label owner identities. It did not copy the score or
mutate routing. The worktree and observer were removed after measurement.

The preceding controlled endpoint-label experiment remains limited to one
endpoint blocker. Parallel, self-loop, manual route, drag, and production
policy are excluded here.

## Population

The sample has 14 Relations: 2 self-loops, 2 parallel ordinary Relations, and
10 primary single ordinary Relations. Five of those ten are straight and five
are curved. Two curved primary Relations had a lower-curvature candidate that
also passed observed non-label safety.

| Relation | chord | current offset | lower candidate | owners | class |
|---|---:|---:|---:|---|---|
| authority-lighthouse | 121.9 | -24 | 0 | lighthouse | Q4 foreign label |
| sofia-archive | 221.6 | -156 | 60 | clara, archive | Q5 multiple labels |

Thus Q1 source-endpoint single = 0, Q2 target-endpoint single = 0, Q3 both
endpoints = 0, Q4 = 1, Q5 = 1, Q6 = 0, Q7 = 3 curved relations without a
qualifying lower candidate, and Q8 = 0. Strong Q1/Q2 cases are zero, so no
one-shot label move is authorized or performed.

## Required local cases

`clara-thomas-mentors` has chord 163.9, current offset -52, lower candidate
40, and observed owners `lighthouse`, `clara`, and `archive`. `clara-thomas-
supervises` has the same chord, current offset -76, lower candidate 52, and
the same owners. Both are Q5 multiple-label cases, not the controlled
single-endpoint pattern.

`sofia-archive` is the multiple-label real-scene example. `authority-
lighthouse` has a single owner but that owner is foreign to the Relation; it
is not an endpoint-label opportunity.

## Result

This is **CASE 10** / `MIXED — LIGHTHOUSE CURVED ROUTES INCLUDE MULTI-LABEL
AND FOREIGN-LABEL BLOCKERS, WHILE THE CONTROLLED SINGLE-ENDPOINT PATTERN DOES
NOT EXPLAIN THE CLARA / THOMAS CASE`.

The controlled single-endpoint mechanism is not established in current
Lighthouse, does not explain Clara/Thomas, and does not justify an interactive
preview or production change. The selected next checkpoint is
`E2R-LIAISONSCAPE-AUTOMATIC-ROUTE-MULTI-LABEL-CORRIDOR-ISOLATION1`.

Parallel and self-loop policies remain unchanged. EN comparison was not run:
there were no Japanese Q1/Q2 successes to compare.
