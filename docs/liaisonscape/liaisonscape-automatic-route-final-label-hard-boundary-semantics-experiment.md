# LiaisonScape Automatic-Route Final-Label Score-Boundary Semantics Experiment

Date: 2026-08-31

Status: bounded application experiment; non-normative

## Exact result

`BLOCKED — THE JA CASCADE CANNOT YET BE RELIABLY ATTRIBUTED TO HARD OR HALO SCORE SEMANTICS`

The checkpoint name is historical; it does not establish a hard-boundary
result. The preceding experiment established a valid geometric causal edge:
reverting the Pass-2 Clara final Node-label rectangle to Pass-1 geometry
removes all four Japanese Pass-3 Clara-centred route changes. That result
remains valid. It does not reveal the candidate-score term that selects each
winner.

## Attempted attribution and failure boundary

The diagnostic held endpoints, the current candidate space, Pass-3 predecessor
paths, and all labels except Clara fixed, then enumerated candidate geometry
under Clara L0 (Pass 1) and L1 (Pass 2). It separated Node, Clara hard,
Clara halo, other-label, occupied, and offset contributions.

That reconstruction did **not** reproduce all four live Pass-3 winners. The
remaining live behavior depends on router-internal details that must remain
exact for attribution: the side-preserving safe-candidate fallback and its
strict safe predicate, sustained occupied-path-nearness scan, canonical
predecessor geometry, and strict candidate-order comparison. A partial score
copy can make an apparent hard/halo result false.

The temporary diagnostic was removed. No Production diagnostic API was added,
and no score, candidate, order, label, or routing behavior changed.

## What is known and what is not

Known:

- JA Pass 2 moves Clara's final Node label by `100.31` units.
- With actual Pass-3 predecessor paths held, substituting only Clara L0 for L1
  removes `clara-lighthouse`, `clara-maya`, `clara-thomas-mentors`, and
  `clara-thomas-supervises` changes: **4/4**.
- JA reaches a fixed state after that second layer; no cycle was observed.

Not established:

- hard `0 ↔ 100000` transition for any of the four winners;
- halo entry or magnitude as a direct trigger;
- occupied-path necessity once exact score semantics are held;
- candidate tie/order contribution;
- whether a hypothetical hard rejection protects genuine final presentation or
  is a proxy/migrating conflict.

Therefore every target remains **SB7 / unresolved**, hard direct trigger count
`0/4 established`, halo direct trigger count `0/4 established`, and tie effect
`0/4 established`. These are absence-of-evidence counts, not evidence that the
terms are absent.

## Decision

Production implementation, score tuning, hard-penalty change, halo change,
iterative solving, route-order change, and router redesign are justified now:
**NO**. The next checkpoint is not a policy experiment. It must first provide a
behavior-identical, temporary score trace for the exact live candidate loop:

`E2R-LIAISONSCAPE-AUTOMATIC-ROUTE-FINAL-LABEL-SCORE-TRACE-REPRODUCTION1`.

Only after that trace reproduces each live winner may hard/halo semantics be
attributed.

## Closure

Production Runtime, routing, route order, label placement, candidate set,
drag behavior, samples, schema, and `seededPositions` changed: **NO**. No
LiaisonScape commit, push, tag, release, deployment, or publication occurred.
Temporary diagnostics were removed and existing Lighthouse EN/JA sample edits
were preserved.
