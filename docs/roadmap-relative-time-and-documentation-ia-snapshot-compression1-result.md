# Relative Time / Documentation IA Snapshot Compression 1

Date: 2026-09-24

Status: **BOUNDED ROADMAP POINTER COMPRESSION COMPLETE — NO AUTHORITY OR
SEMANTIC CHANGE**

## Scope and authority

This checkpoint compressed superseded planning/preparation prose in
`docs/roadmap.md` into pointers to the existing decision, adoption, audit, and
migration authorities. It did not edit those authorities, move or rename
files, alter History units, or make a new Relative Time or Documentation IA
decision. The Roadmap remains the sole current-planning authority.

## Compressed snapshots

- The 2026-09-23 interval-semantics / Timeline section now points atomic
  semantics to the Human adoption record. Its separate, non-persisted explicit
  Timeline-placement direction and non-authorization boundary remain visible.
- The extensible-vocabulary section now points to the Human-selected direction
  and atomic adoption records; pre-adoption status and next-research wording is
  marked historical rather than current.
- The Relative Time-to-Documentation IA sequence and the earlier IA review
  direction now point to their decision/readiness/adoption and
  decision-preparation authorities. The superseded sequence and inventory
  prose remains recoverable from the source Roadmap at parent commit
  `629937741b7422e30abb38dbafb6dd821605187c`; the dedicated documents remain
  the authorities for their decisions and evidence.
- The earlier Roadmap-first selection and pre-migration preparation snapshots
  now point to the structure decision, implementation-preparation record, and
  current migration result. The current Roadmap-first direction and chronology
  migration status remain as concise planning entries.
- The current Relative Time status now identifies the adoption as authority,
  preserves the Draft/schema and implementation boundary by reference, and
  records the atomic phase as closed after minimum state synchronization.

## Retained current boundaries

The atomic adoption's normative scope and exclusions are unchanged. In
particular, Draft/schema support, representation/versioning, machine
evaluation, boundary and History mapping, cross-assertion policy, Derived
semantics, Validator, migration, and application work are not implicitly
authorized. The Human-selected Roadmap-first direction remains current;
the wider `docs/` taxonomy and relocation remain deferred. Current chronology
migration progress and any unverified mixed/open Roadmap content remain visible
through the existing current-status/migration entries; this compression does
not claim that all historical-looking material has been migrated.

The historical headings were retained, preserving their anchors. No links,
heading identities, History files, application code, schema, Validator,
runtime, tests, or sibling repositories were changed by this checkpoint.
