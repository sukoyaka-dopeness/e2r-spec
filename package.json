{
  "name": "e2r-spec",
  "version": "0.0.0",
  "private": true,
  "description": "Development tooling for the E2R specification.",
  "scripts": {
    "validate": "npm run validate:history",
    "validate:history": "node scripts/validate-history-schema.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
