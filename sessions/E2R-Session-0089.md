# E2R Session 0089

Objective: Product Node-label Hysteresis / Recovery Attribution Experiment 1.

Checkpoint: distinguish previous-placement continuity from fresh Product
Node-label quality, and test a bounded recovery semantic without changing
Product routing, Relation-label, endpoint-plan, Structural Placement, or
Self-loop authority.

Started: 2026-09-16.

Status: `A QUALIFIED FOR CONTINUED BOUNDED NODE-LABEL RESEARCH / PRODUCTION NOT ESTABLISHED`.

Continuation file: This is a new objective after Session 0088 under the
workspace rule that one continuous purpose uses one session file and a
materially different checkpoint uses a new file.

## Scope

Inspect the current Product `placeNodeLabel` movement cost and the App
previous-placement lifecycle. Compare current-fresh, current-previous,
hysteresis-ablation, bounded-recovery, active-drag, and finalizing-drag arms
on the previous eight-fixture control set. Keep Relation routes and
Relation-label rectangles fixed. Do not change Dataset or authored/stored
Coordinate semantics, persistence, dirty-state, Save Coordinates, manual
placement, Product defaults, provider status, or Human Review status.

## Work performed

- inspected `placementMovementCost`, previous Node-label retention, active and
  finalizing drag state, feedback-pass ordering, and previous snapshot update;
- added additive candidate trace fields for fresh/continuity rank, previous and
  selected identities, movement, hard/halo pressure, yielding-route pressure,
  hard-safe classification, and Relation/Node occupied-label split;
- added development-only App seams for a previous Node-label snapshot, a final
  diagnostic Node-label override, and movement-cost ablation;
- reused the previous checkpoint's Product `current-previous` output as the
  main previous snapshot rather than using only a hand-written inward seed;
- generated the bounded eight-fixture artifact with 32 candidates per Node;
- compared fresh, continuity, ablation, recovery, active-drag, and
  finalizing-drag states;
- checked repeated identical-input recovery for stability and oscillation;
- created an Actual Product preview surface and performed a small smoke check
  for primary, high-degree, Self-loop, and Lighthouse EN/JA fixtures.

## Result

Previous placement changed selected directions in every fixture family. The
hysteresis ablation exactly matched fresh selection in all eight fixtures.
The bounded recovery rule returned the fresh labels in all eight fixtures,
with both fresh-gain and hard-conflict triggers, while remaining stable on a
repeated identical derivation. Fixed Relation routes and Relation-label
rectangles remained unchanged, and no Dataset or coordinate mutation was
introduced.

The result is `A QUALIFIED`: explicit recovery is a viable next bounded
Node-label formulation and previous-placement hysteresis is the primary
tested residual. It is not a production provider or default adoption.

## Validation record

- recovery diagnostic artifact regenerated;
- focused hysteresis/angular tests passed 5/5;
- LiaisonScape lint passed;
- LiaisonScape production build passed;
- Actual Product smoke completed for primary, high-degree, Self-loop, and
  Lighthouse EN/JA controls;
- inspected browser console reported no warnings/errors;
- Human Review remains `NOT READY`.

## Standing decisions

- Product default/adoption: `HOLD`;
- production provider: `NOT ESTABLISHED`;
- Human Review: `NOT READY`;
- Initial Layout Release blocker: `OPEN`;
- Parallel/Incident architecture: `CLOSED`;
- Adaptive Initial Placement Cascade: `NOT ENTERED`;
- session log for this objective: this file only; no prior session log was
  rewritten.
